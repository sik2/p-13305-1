spring:
  application:
    name: back
  datasource:
    url: jdbc:h2:./db_article;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  max-http-request-header-size: 64KB
