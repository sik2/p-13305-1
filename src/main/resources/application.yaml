spring:
  application:
    name: p-13305-1

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/article_board?useUnicode=true&characterEncoding=utf8&autoReconnect=true&serverTimezone=Asia/Seoul&useSSL=false
    # 127.0.0.1:3306 ? ? ???(localhost)? MySQL ?? ??
    # article_board ? ??? ?? DB ??
    # useUnicode=true&characterEncoding=utf8 ? ?? ?? ??
    # serverTimezone=Asia/Seoul ? ?? ??? ??
    username: root
    password: 1234 # ????? ??? ??? ??

  jpa:
    hibernate:
      ddl-auto: update # update: ??? ???? DB? ??. ?? ??? ??.
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true