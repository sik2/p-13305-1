spring:
  application:
    name: SpringBoot_test1

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # [DB명] 은 교체
    url: jdbc:mysql://localhost:3306/SpringBoot_test1?useUnicode=true&characterEncoding=utf8&autoReconnect=true&serverTimezone=Asia/Seoul&useSSL=false
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
